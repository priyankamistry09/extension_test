var Built = require('built.io');
/**
 * This middleware wraps the the request body into js sdk wrappers
 */

module.exports = function (req, res, next) {
	req.builtApp = Built.App(req.headers.application_api_key)
	next()
}