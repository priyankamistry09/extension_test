var objects = require('./objects')

module.exports = {
	'/:classUid': {
		before: function (req, res) {
			req.bclass = req.builtApp.Class(req.params.classUid)
			return utils.Promise.resolve()
		},
		"/objects": objects
	}
}